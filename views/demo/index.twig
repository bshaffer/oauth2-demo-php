{% extends "demo/base.twig" %}

{% block content %}
    <h3>Demo App</h3>
    <p>
        We would like to use your information in order to integrate with your friends,
        use your personal information for nefarious purposes, and to make your life better somehow.
    </p>
    <p>
        Click below to integrate with that service you belong to:
    </p>
    <a class="button" href="{{ app.parameters.authorize_route|slice(0, 4) == 'http' ? app.parameters.authorize_route : url(app.parameters.authorize_route) }}?response_type=code&client_id={{app.parameters.client_id}}&redirect_uri={{ url('authorize_redirect')|url_encode() }}">Authorize</a>
{% endblock %}